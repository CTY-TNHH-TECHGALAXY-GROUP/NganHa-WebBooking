# Role B handoff — PostgreSQL CAS/ACL gate and migration preflight

Date: 2026-09-15 (Asia/Ho_Chi_Minh)

## Scope and provenance

- Role: B (SystemConfigs CAS, schema/ACL preflight, real PostgreSQL probe).
- Base SHA: `c964977ca2af3081248c902a626612c4b11722af`.
- Head SHA: report the final commit from `git rev-parse HEAD` after cherry-pick/amend; the commit created in this worktree is reported to the integrator in the handoff message.
- Branch/worktree: `codex/ps-reaccept-b-20260915` / `/private/tmp/nganha-pagespeed-role-b`.
- No production Supabase URL, Storage bucket, database, settings, push, merge, or deploy was contacted.
- Owner files: `supabase/migrations/20260914_system_configs_jsonb_cas.sql`, `scripts/test-cas-concurrency-contract.mjs`, `scripts/test-cas-postgres.mjs`, and this Role B evidence.
- UI/media files were not modified.

## Changes

1. Tightened the reviewed SQL preflight so `SystemConfigs.key` must be `text` and its unique index must be valid, ready, single-column, non-partial, non-expression, and exactly on `key`. A composite or partial unique index no longer satisfies the gate.
2. Added invoker table-privilege preflight for `service_role` (`SELECT`, `INSERT`, `UPDATE`) and a forced-RLS safety check requiring `service_role` to bypass forced row security. The function remains narrowly allowlisted and `anon`/`authenticated` execute grants remain revoked.
3. Removed the previous JavaScript `Map` concurrency simulation from `scripts/test-cas-concurrency-contract.mjs`. It now asserts the source contract only and explicitly directs the real DB gate to `scripts/test-cas-postgres.mjs`.
4. Added `scripts/test-cas-postgres.mjs`, a fail-closed runner that accepts only an explicit loopback PostgreSQL URL, creates a random disposable database, applies the reviewed migration, and records real function ACL, table ACL/RLS, strict unique-index, JSONB equality, stale conflict, invalid payload/key, role denial, independent two-transaction race, rollback/read-back, rollback conflict, and absent-row insert conflict results. It drops the disposable database and only drops roles created by the runner.

## Verification

| Command | Exit | Result |
| --- | ---: | --- |
| `node --check scripts/test-cas-postgres.mjs` | 0 | Runner syntax valid. |
| `node --check scripts/migrate-pagespeed-renditions.mjs` | 0 | Existing migration syntax valid. |
| `node scripts/test-cas-concurrency-contract.mjs` | 0 | Source-contract assertions only; explicitly not a DB acceptance result. |
| `node scripts/test-cas-postgres.mjs --tested-sha=c964977... --evidence=.../cas-postgres-report.json` | 2 | `NOT_VERIFIED`: no explicit disposable PostgreSQL URL was available. |
| `PAGESPEED_TEST_DATABASE_URL=postgresql://127.0.0.1:5432/postgres node scripts/test-cas-postgres.mjs ...` | 2 | `NOT_VERIFIED`: project `pg` package is not installed in this worktree. No connection attempted. |
| `node scripts/test-pagespeed-rendition-safety.mjs` | 1 | Environment dependency failure: `migrate-pagespeed-renditions.mjs` cannot import `sharp` because this isolated worktree has no `node_modules`; no Supabase call occurred. |
| `git diff --check` | 0 | No whitespace errors. |

## Evidence

- `cas-postgres-report.json`: raw runner output for this worktree, status `NOT_VERIFIED`, tested SHA `c964977...`, timestamp `2026-09-15T10:48:10Z`.
- `evidence-index-20260915.json`: redacted index with SHA-256 hashes and command provenance.
- No credentials, customer configuration, database URL, or raw backup was written to the repository.

## Gate status and residuals

- Source CAS contract/preflight: PASS locally.
- Real PostgreSQL ACL/RLS/concurrency/read-back: **NOT VERIFIED** — no disposable PostgreSQL server/client was available.
- Migration crash after upload, verify failure, crash/restart/resume, rerun, durable backup restore, and two-run rollback: **NOT VERIFIED**. The existing migration still fail-closes an `apply_partial` run and does not claim resume support; this handoff does not falsely mark those cases complete.
- API mandatory `expectedRevision` and CAS RPC wiring were audited as present at the base; no API writer changes were needed in this role.
- Production/live schema and RPC availability: **NOT VERIFIED** and intentionally not probed.

## Integration and rollback

- Integrator may cherry-pick this single scoped commit after reviewing the SQL and runner. Re-run the real runner from the integrated SHA with dependencies installed and a disposable loopback PostgreSQL instance.
- If the SQL preflight rejects the target schema, stop; do not loosen it or bypass the unique-index/ACL/RLS checks. Resolve schema/privilege drift in a reviewed migration proposal.
- Code rollback is a scoped revert of this commit. The SQL migration makes no content change; the runner's disposable database and created roles are cleaned up in `finally`.
